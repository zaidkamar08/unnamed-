vector<vector<int>>result;
void solve(vector<int>&nums,int i,vector<int>temp){
    if(i>=nums.size()){
        result.push_back(temp);
        return;

    }
    temp.push_back(nums[i]);
    solve(nums,i+1,temp);
    temp.pop_back();
    solve(nums,i+1,temp);
}
    vector<vector<int>> subsets(vector<int>& nums) {
    vector<int> temp;
    solve(nums,0,temp);
    return result;

just two changes 

after declaring temp
sort the array like 
sort(nums.begin(),nums.end());

and change 2 

ignore the double element while passing 
after poping the element
write this while statement 
while( i+1< nums.size() && nums[i]==nums[i+1]){
i++;
}

rest code all same
